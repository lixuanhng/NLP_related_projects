# 字符串相乘



```python
# 两个字符串相乘，双指针从字符串末尾开始
# case: s1: 12, s2: 11
def mulply(self, s1, s2):
    n1, n2 = len(s1), len(s2)
    carry = 0
    res = ''
    # p1, p2 = n1-1, n2-1  # 指针
    for i in range(n1-1, 0, -1):
            if i < n-1 and i - 1 > -1:
	            tmp = (s1[i] + s1[i-1]) * s2[i-1] + carry
        
        
```

